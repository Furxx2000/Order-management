# Modern Order Management Dashboard

這是一個使用 React (Vite) 和 TypeScript 建構的現代化訂單管理儀表板。專案採用了最新的前端技術，旨在提供一個功能豐富、高效且具備良好使用者體驗的介面。

此專案不僅包含一個完整的訂單管理系統，還整合了資料分析與元件化開發的範例。

---

## ✨ 主要功能 (Features)

### 1. 訂單管理系統 (Order Management System)

這是本專案的核心功能，一個仿照真實後台情境的訂單管理介面。

- **動態資料表格**：使用 TanStack Table v8 打造，支援前端分頁、篩選、排序等功能。
- **即時搜尋與篩選**：可依訂單編號、用戶名稱進行即時搜尋，並提供狀態篩選器。
- **狀態更新**：可在表格中直接更新訂單的配送狀態，並包含對應的業務邏輯驗證。
- **響應式設計 (RWD)**：在行動裝置上，表格會自動轉換為易於閱讀的卡片式版面配置。
- **即時統計**：頁面頂部會根據當前篩選結果，顯示即時的統計數據（如總筆數、總金額）。
- **API Mocking**: 透過 MSW (Mock Service Worker) 模擬後端 API，使前端能獨立開發與測試。

### 2. 資料分析模組 (Data Analysis Module)

此模組展示了在前端進行複雜資料處理與運算的能力。應用程式中提供了一個互動介面，可以觸發預設的分析腳本，並將結果視覺化呈現。

> **參考**: 原始的獨立腳本位於 `task/userOrders-processing.js`，展示了如何在無 UI 環境下執行相同的邏輯。

### 3. 待辦事項元件 (Todo List Component)

一個經典的待辦事項列表，作為 React 元件化的實踐範例。

> **參考**: 原始的 HTML/JS 版本位於 `task/todo.html`，用於對比與展示重構過程。

---

## 🚀 快速開始 (Getting Started)

請依照以下步驟在您的本機環境啟動此專案。

1.  **Clone 專案**

    ```bash
    git clone <repository-url>
    cd <repository-directory>
    ```

2.  **安裝依賴套件**

    ```bash
    npm install
    ```

    或若您使用 `bun`:

    ```bash
    bun install
    ```

3.  **啟動開發伺服器**

    ```bash
    npm run dev
    ```

4.  開啟瀏覽器並訪問 `http://localhost:5173` (或終端機提示的位址)。

---

## 🛠️ 技術堆疊 (Tech Stack)

- **框架**: React (Vite)
- **語言**: TypeScript
- **UI**: Tailwind CSS, shadcn/ui (Radix UI)
- **表格**: TanStack Table v8
- **API Mocking**: Mock Service Worker (MSW)
- **狀態管理**: React Hooks, Context API

---

## 📂 專案結構 (Project Structure)

```
/
├── task/                     # 包含原始的 JS/HTML 腳本，供參考
├── src/
│   ├── components/           # React 元件 (包含 UI 元件及功能元件)
│   ├── services/             # API 請求相關模組
│   ├── lib/                  # 共用函式、類型定義
│   ├── hooks/                # 自定義 Hooks
│   ├── mocks/                # MSW (Mock Service Worker) 設定
│   └── App.tsx               # 主應用程式進入點與路由
│
├── package.json              # 專案設定與依賴
└── vite.config.ts            # Vite 設定檔
```

---
